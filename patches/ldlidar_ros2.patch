diff --git a/CMakeLists.txt b/CMakeLists.txt
index 96ff2d2..6243c6a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -15,7 +15,7 @@ if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
   add_compile_options(-Wall -Wextra -Wpedantic)
 endif()
 
-# find dependencies
+# Find dependencies
 find_package(ament_cmake REQUIRED)
 find_package(rclcpp REQUIRED)
 find_package(sensor_msgs REQUIRED)
@@ -23,41 +23,46 @@ find_package(geometry_msgs REQUIRED)
 
 if(BUILD_TESTING)
   find_package(ament_lint_auto REQUIRED)
-  # the following line skips the linter which checks for copyrights
-  # uncomment the line when a copyright and license is not present in all source files
-  #set(ament_cmake_copyright_FOUND TRUE)
-  # the following line skips cpplint (only works in a git repo)
-  # uncomment the line when this package is not in a git repo
-  #set(ament_cmake_cpplint_FOUND TRUE)
   ament_lint_auto_find_test_dependencies()
 endif()
 
-# user add
+# LD19 SDK headers
 include_directories(
   ${CMAKE_CURRENT_SOURCE_DIR}/include
   ${CMAKE_CURRENT_SOURCE_DIR}/sdk/include/
 )
 
-add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/sdk)
+# Build LDROBOT SDK
+add_subdirectory(
+  ${CMAKE_CURRENT_SOURCE_DIR}/sdk
+)
 
-add_executable(${PROJECT_NAME}_node  
-  ${CMAKE_CURRENT_SOURCE_DIR}/src/demo.cpp 
+# Build ROS 2 node
+add_executable(
+  ${PROJECT_NAME}_node
+  ${CMAKE_CURRENT_SOURCE_DIR}/src/demo.cpp
 )
-ament_target_dependencies(${PROJECT_NAME}_node rclcpp sensor_msgs geometry_msgs)
-target_link_libraries(${PROJECT_NAME}_node 
+
+# ROS 2 Lyrical-compatible linking
+target_link_libraries(
+  ${PROJECT_NAME}_node
+  rclcpp::rclcpp
+  ${sensor_msgs_TARGETS}
+  ${geometry_msgs_TARGETS}
   pthread
   ldlidar_driver
 )
 
-# Install 
-install(TARGETS ${PROJECT_NAME}_node
- DESTINATION lib/${PROJECT_NAME}
+# Install executable
+install(
+  TARGETS ${PROJECT_NAME}_node
+  DESTINATION lib/${PROJECT_NAME}
 )
 
-install(DIRECTORY launch rviz2 scripts
+# Install launch/RViz/scripts
+install(
+  DIRECTORY launch rviz2 scripts
   DESTINATION share/${PROJECT_NAME}/
 )
 
-
-ament_package()
-
+ament_package()
\ No newline at end of file
diff --git a/launch/ld19.launch.py b/launch/ld19.launch.py
index 16daa33..198e46e 100644
--- a/launch/ld19.launch.py
+++ b/launch/ld19.launch.py
@@ -33,7 +33,7 @@ def generate_launch_description():
         {'laser_scan_topic_name': 'scan'},
         {'point_cloud_2d_topic_name': 'pointcloud2d'},
         {'frame_id': 'base_laser'},
-        {'port_name': '/dev/ttyUSB0'},
+        {'port_name': '/dev/ttyUSB1'},
         {'serial_baudrate': 230400},
         {'laser_scan_dir': True},
         {'enable_angle_crop_func': False},
@@ -49,7 +49,16 @@ def generate_launch_description():
     package='tf2_ros',
     executable='static_transform_publisher',
     name='base_link_to_base_laser_ld19',
-    arguments=['0','0','0.18','0','0','0','base_link','base_laser']
+    arguments=[
+    '--x', '0.114',
+    '--y', '0.0',
+    '--z', '0.10977',
+    '--roll', '0.0',
+    '--pitch', '0.0',
+    '--yaw', '-1.57079632679',
+    '--frame-id', 'base_link',
+    '--child-frame-id', 'base_laser',
+    ]
   )
 
 
diff --git a/sdk b/sdk
--- a/sdk
+++ b/sdk
@@ -1 +1 @@
-Subproject commit 6148cd3c33ae39adbd7e12faf82567fa8f8e7b0d
+Subproject commit 6148cd3c33ae39adbd7e12faf82567fa8f8e7b0d-dirty
